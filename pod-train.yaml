apiVersion: v1
kind: Pod
metadata:
 name: unitopatho-generative-generate-testset
spec:
  restartPolicy: Never
  containers:
    - name: train
      image: daviderubinetti/unitopatho-generative:latest
      resources:
        limits:
          nvidia.com/gpu: 1
        requests:
          nvidia.com/gpu: 1
      volumeMounts:
            - name: unitopatho #this is just a local name
              mountPath: /data
  volumes:
      - name: unitopatho
        persistentVolumeClaim:
          claimName: unitopatho-shared
